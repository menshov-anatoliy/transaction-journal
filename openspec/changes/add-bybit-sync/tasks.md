# Tasks

## 1. Каркас и сырая схема

- [x] 1.1 Убедиться, что каркас решения существует (solution, хост-проект, EF Core + SQLite, Polly); при отсутствии — создать минимальный по стеку карты, проверить `dotnet build`
- [x] 1.2 Создать EF-модели сырого хранилища (`RawExecution` c уникальным индексом по `execId`, `RawDelivery` по `symbol + deliveryTime`, `RawInstrument`, `SyncRun`/`SyncState`) и миграцию; проверить применение миграции к пустой SQLite-базе
- [x] 1.3 Ввести интерфейс поставщика API-ключа/секрета (конкретное место хранения — решение тикета #4) и dev-реализацию из переменных окружения; проверить unit-тестом подстановку ключа в запрос

## 2. Bybit-клиент

- [x] 2.1 Реализовать HttpClient-обёртку с HMAC-SHA256 (hex lower, ручная сборка queryString, `recv_window`, timestamp, сверка с `/v5/market/time`); проверить unit-тестами подпись на примерах официального C#-примера Bybit
- [x] 2.2 Добавить read-only методы `GET /v5/execution/list`, `GET /v5/asset/delivery-record`, `GET /v5/market/instruments-info`, `GET /v5/market/time` с типизированными ответами; проверить интеграционными тестами против зафиксированных HTTP-ответов
- [x] 2.3 Настроить resilience: минимальный интервал между запросами, учёт `X-Bapi-Limit-*`, Polly-ретраи (сеть/5xx — короткий бэкофф; `retCode 10006` — пауза секунды; HTTP 403 — длинная пауза с ограничением попыток); проверить unit-тестами на фиктивном транспорте все четыре ветки

## 3. Sync engine

- [x] 3.1 Реализовать проход 7-дневного окна с курсорной пагинацией и ранней остановкой, когда страница целиком из известных `execId`; проверить unit-тестом на фиктивном шлюзе с многостраничным ответом
- [x] 3.2 Реализовать выбор режима по водяному знаку (backfill при первом запуске, инкремент с перекрытием далее) и фиксацию достигнутой границы backfill по каждой категории (`linear`, `option`); проверить unit-тестами оба перехода режима
- [x] 3.3 Реализовать идемпотентную вставку сырых записей пачками с ведением прогресса в `SyncRun`; проверить тестом: повторный прогон после «обрыва» на середине продолжает с места остановки и не создаёт дублей
- [x] 3.4 Реализовать delivery-проход 30-дневными окнами с дедупом `symbol + deliveryTime`; проверить unit-тестом на пересекающихся окнах

## 4. Материализатор

- [x] 4.1 Построить справочник инструментов из `instruments-info` (категория, `optionsType`, `baseCoin`, `deliveryTime`) и парсер символа опциона `{BASE}-{dMMMyy}-{strike}-{C|P}` (InvariantCulture) со сверкой со справочником; проверить unit-тестами на реальных символах BTC/ETH
- [x] 4.2 Выводить сделки из `RawExecution` во «Входящие» (знак по `side`, `execPrice`, `execFee` со знаком и `feeCurrency`, USDC ≡ USDT по ADR-0001); проверить unit-тестами сценарий «инкрементальная догрузка приводит новые сделки»
- [x] 4.3 Выводить закрывающие записи экспираций: ITM-деление аккаунтовой delivery пропорционально остаткам конструкций, OTM-закрытие по нулевой цене в `deliveryTime` при отсутствии delivery-записи, предупреждение сверки с `deliveryRpl`; проверить unit-тестами три сценария требования спеки
- [ ] 4.4 Обеспечить полный переразбор доменных представлений из сырых записей без сетевых запросов; проверить тестом: пересборка после изменения правила разбора даёт согласованный результат

## 5. UI-точка входа и приёмка

- [ ] 5.1 Сделать минимальную страницу с кнопкой «Синхронизировать», индикацией статуса/прогресса, счётчиками новых записей и предупреждениями сверки; проверить bUnit-тестом реакции на завершение синка (полный экран — отдельная capability UI)
- [ ] 5.2 Прогнать приёмочный чек-лист по всем сценариям `specs/sync/bybit-history/spec.md` на тестовых данных и зафиксировать результат; считать change готовым к архивации при полном прохождении
