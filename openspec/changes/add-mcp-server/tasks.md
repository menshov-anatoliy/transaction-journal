# Tasks

## 1. Каркас режима `--mcp`

- [ ] 1.1 Раннее ветвление по аргументу `--mcp` до `WebApplication.CreateBuilder`: generic host, `AddMcpServer` + `WithStdioServerTransport`, регистрация tools; проверить сценарии «Аргумент `--mcp` поднимает сервер вместо веб-приложения» и «Без `--mcp` процесс остаётся веб-приложением»; заодно проверить генерацию схем для enum/DateTime (открытый вопрос research)
- [ ] 1.2 Гигиена вывода: все логи в stderr (`LogToStandardErrorThreshold`), страховка `Console.SetOut(Console.Error)`; проверить сценарий «Логи не попадают в stdout»
- [ ] 1.3 Завершение по EOF stdin без фоновых сервисов, сопротивляющихся остановке; проверить сценарий «EOF stdin останавливает сервер»

## 2. Read-only доступ к общей базе

- [ ] 2.1 Соединения `Mode=ReadOnly` + `Pooling=true` без `Cache=Shared`; параллельная работа MCP-читателя с пишущим веб-приложением; проверить сценарии «Читатель работает параллельно пишущему веб-приложению» и «MCP-режим не создаёт и не мигрирует базу»
- [ ] 2.2 Обработка отсутствующего файла базы: понятная MCP-ошибка на русском вместо аварийного завершения; проверить сценарий «Отсутствующий файл базы — понятная ошибка»

## 3. Tools

- [ ] 3.1 `list_constructions`: список конструкций со сводкой (статус, капитал, реализованный/нереализованный/общий PnL, %, даты); проверить часть сценария «Все шесть tools зарегистрированы»
- [ ] 3.2 `get_construction`: конструкция + позиции + сделки до 200 с `truncated`/`total_count` + внешние корректировки PnL с разбивкой результата; проверить сценарий «Переполнение сделок в get_construction помечается»
- [ ] 3.3 `list_trades`: плоский журнал сделок с фильтрами `construction_id`, `date_from`/`date_to`, `instrument`; проверить сценарий «Фильтры сужают выборку»
- [ ] 3.4 `get_positions`: открытые позиции с нереализованной оценкой и фильтром `instrument`; проверить сценарий «Оценка сопровождается временем марок» применительно к позициям
- [ ] 3.5 `list_incoming`: сделки «Входящих» для триажа с ИИ; проверить сценарий «Мутации журнала через MCP недоступны»
- [ ] 3.6 `get_analytics`: итоги журнала, помесячная серия PnL, топ/анти-топ конструкций; проверить сценарий «Только tools, без resources и prompts»

## 4. Форматы и ошибки

- [ ] 4.1 Форматирование DTO: поля snake_case, русский контент, точность итогов 2 знака и количеств/цен 8 знаков, время ISO 8601 UTC, пустая выборка — `[]`; проверить сценарии «Поля английские, контент русский», «Числа с фиксируемой точностью», «Время в ISO 8601 UTC», «Пустая выборка — пустой массив»
- [ ] 4.2 Пагинация `limit`/`offset` (дефолт 50, максимум 500) и фиксированная сортировка свежими первыми; проверить сценарий «Дефолт и максимум пагинации»
- [ ] 4.3 `McpException` на русском для неизвестного `construction_id`, generic-ответ для багов; проверить сценарии «Неизвестный construction_id — ошибка MCP» и «Баг не раскрывает деталей»
- [ ] 4.4 Нереализованный PnL по маркам на момент запроса с `marks_as_of` и деградацией при сбое марок (делегирование провайдеру марок `add-analytics`); проверить сценарий «Сбой марок обнуляет только нереализованную часть»

## 5. Приёмка

- [ ] 5.1 Задокументировать конфиги регистрации клиентов (VS Code `.vscode/mcp.json`, Claude Desktop `claude_desktop_config.json`, Cursor `.cursor/mcp.json`): `command` — путь к exe, `args` = `["--mcp"]`
- [ ] 5.2 Прогнать приёмочный чек-лист по всем сценариям `specs/mcp/tools/spec.md` и зафиксировать результат; подтвердить аддитивность контракта; считать change готовым к архивации при полном прохождении
