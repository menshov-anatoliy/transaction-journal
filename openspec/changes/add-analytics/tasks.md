# Tasks

## 1. Провайдер марок

- [x] 1.1 Реализовать HTTP-клиент публичного эндпоинта тикеров без аутентификации и API-ключа; проверить сценарий «марки получаются без аутентификации»
- [x] 1.2 Реализовать кэш последней известной марки (инструмент, цена, время получения) с EF-моделью и миграцией; проверить сценарии «кэш марок штампуется временем» и «последняя известная марка служит дефолтом ручной пометки»

## 2. Движок FIFO

- [x] 2.1 Реализовать сопоставление FIFO по единому хронологическому потоку записей позиции (сделки + закрывающие записи с эффективными ценами); проверить сценарии «встречные записи закрывают старейшие открытые части» и «закрывающая запись delivery входит в расчёт по внутренней стоимости»
- [x] 2.2 Включить комиссии записей исполнения в результат со знаком уменьшения, USDC — по паритету 1:1; проверить сценарий «комиссии исполнений уменьшают результат»
- [x] 2.3 Подтвердить исключение funding-начислений и подчинённость `deliveryRpl` сверке; проверить сценарий «funding не входит в результат конструкции» и предупреждающую сверку без подмены расчёта

## 3. Метрики позиции

- [x] 3.1 Реализовать метрики позиции: остаток, реализованный PnL, накопленные комиссии, даты открытия/закрытия, длительность; проверить сценарии «позиция показывает агрегаты своих записей», «закрытая позиция не имеет нереализованной части» и «переоткрытие позиции сдвигает её даты»

## 4. Метрики конструкции

- [ ] 4.1 Реализовать итог конструкции как сумму позиций и внешних корректировок PnL с процентами от текущего выделенного капитала; проверить сценарии «итог конструкции включает корректировки» и «проценты считаются от текущего капитала»
- [ ] 4.2 Реализовать производные даты конструкции и длительность, для открытой — до текущего момента; проверить сценарии «даты конструкции выводятся из записей» и «длительность открытой конструкции считается до текущего момента»

## 5. Нереализованная оценка и деградация

- [ ] 5.1 Реализовать оценку нереализованного PnL открытых остатков по маркам на момент запроса с отметкой `marks_as_of`; проверить сценарий «открытый остаток оценён маркой на момент запроса»
- [ ] 5.2 Реализовать деградацию при недоступности марок: null только у нереализованной части и отметки времени; проверить сценарий «сбой марок обнуляет только нереализованную часть»

## 6. Производность и приёмка

- [ ] 6.1 Подтвердить вычисление метрик при чтении без хранимых результатов: правки привязок, корректировок, пометок и капитала отражаются очередным чтением; проверить сценарии «правка входных данных отражается при следующем чтении», «кэш вычислений прозрачен» и «перенос сделки пересчитывает результат обеих конструкций»
- [ ] 6.2 Прогнать приёмочный чек-лист по всем сценариям `specs/analytics/performance/spec.md` и зафиксировать результат; считать change готовым к архивации при полном прохождении
